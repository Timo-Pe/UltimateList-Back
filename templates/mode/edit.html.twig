{% extends 'base.html.twig' %}

{% block title %}Edit Mode{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1>Edit Mode</h1>

        {{ include('mode/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-success"  href="{{ path('app_mode_index') }}">back to list</a>

        {{ include('mode/_delete_form.html.twig') }}
    </div>
{% endblock %}
