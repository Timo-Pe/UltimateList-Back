{% extends 'base.html.twig' %}

{% block title %}Editer le Mode{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1>Editer le Mode</h1>

        {{ include('mode/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        <a class="btn btn-success"  href="{{ path('app_mode_index') }}">Retour à la liste</a>

        {{ include('mode/_delete_form.html.twig') }}
    </div>
{% endblock %}
