{{ form_start(form) }}
    {{ form_widget(form) }}

    <a class="btn btn-sm btn-warning" href="{{ path('app_item_index') }}"><ion-icon name="return-down-back-outline"></ion-icon></a>

    <button class="btn btn-success btn-sm" ><ion-icon name="checkmark-outline"></ion-icon></button>

    {{ include('item/_delete_form.html.twig') }}
{{ form_end(form) }}
