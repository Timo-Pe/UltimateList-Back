{% extends 'base.html.twig' %}

{% block title %}Edit Item{% endblock %}

{% block body %}
<div class="container my-4">
    <h1>Editer l'Item</h1>

    {{ include('item/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <a class="btn btn-success" href="{{ path('app_item_index') }}">Retour à la liste</a>

    {{ include('item/_delete_form.html.twig') }}
</div>
{% endblock %}
