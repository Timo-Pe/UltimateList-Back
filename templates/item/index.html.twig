{% extends 'base.html.twig' %}

{% block title %}Item{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1>Liste des Items</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Description</th>
                    <th>Sortie</th>
                    <th>Producteur</th>
                    <th>Auteur</th>
                    <th>Animateur</th>
                    <th>Developpeur</th>
                    <th>Editeur</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for item in items %}
                <tr>
                    <td class = "align-middle">{{ item.id }}</td>
                    <td class = "align-middle">{{ item.name }}</td>
                    <td class = "align-middle">{{ item.description|length > 70 ? item.description|slice(0, 70) ~ '...' : item.description  }}</td>
                    <td class = "align-middle">{{ item.releaseDate ? item.releaseDate|date('Y-m-d') : '' }}</td>
                    <td class = "align-middle">{{ item.productor }}</td>
                    <td class = "align-middle">{{ item.autor }}</td>
                    <td class = "align-middle">{{ item.host }}</td>
                    <td class = "align-middle">{{ item.developer }}</td>
                    <td class = "align-middle">{{ item.editor }}</td>
                    <td class = "align-middle">
                        <img src={{ item.image }} width="120" height ="120">
                    </td>
                    <td class = "align-middle">
                        <a class="btn btn-primary" href="{{ path('app_item_show', {'id': item.id}) }}">Voir</a>
                        <a class="btn btn-warning" href="{{ path('app_item_edit', {'id': item.id}) }}">Editer</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="11">Pas d'Items trouvés</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a class="btn btn-success" href="{{ path('app_item_new') }}">Créer un nouvel Item</a>
    </div>
{% endblock %}
